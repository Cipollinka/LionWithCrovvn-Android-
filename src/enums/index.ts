export enum Theme {
  dark = 'dark',
  light = 'light',
}

export enum GameMode {
  standart = 'standart',
  compliance = 'compliance',
}

export enum GameSubMode {
  oneTime = 'one-time',
  normal = 'normal',
}

export const LIFE_COUNT = 3;
